<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GUAJIRO · Congelados</title>
  <link rel="icon" type="image/png" href="guajiro-icon.png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600;700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      /* Paleta GUAJIRO - Modo Claro */
      --fondo-principal: #F2F2F2;
      --fondo-card: #FFFFFF;
      --texto-principal: #0D111B;
      --texto-secundario: #39546A;
      --texto-muted: #8A8D91;
      --acento: #39546A;
      --acento-claro: #A7D6E8;
      --acento-hover: #2d4357;
      --navbar-bg: #16202D;
      --navbar-texto: #F2F2F2;
      --footer-bg: #16202D;
      --footer-texto: #A7D6E8;
      --borde-suave: rgba(0, 0, 0, 0.08);
      --sombra-card: 0 3px 12px rgba(0, 0, 0, 0.06);
      --sombra-card-hover: 0 14px 28px rgba(0, 0, 0, 0.12);
      --badge-bg: #A7D6E8;
      --badge-texto: #0D111B;
      --btn-flotante-bg: #39546A;
      --btn-flotante-texto: #F2F2F2;
      --btn-flotante-hover: #2d4357;
      --precio-pill-bg: #16202D;
      --precio-pill-texto: #A7D6E8;
      --toast-bg: #16202D;
      --toast-texto: #F2F2F2;
      --toast-border: #A7D6E8;
      --skeleton-base: #e0e0e0;
      --skeleton-shine: #f0f0f0;
      --categoria-inactiva: #8A8D91;
      --categoria-hover: #A7D6E8;
      --categoria-activa: #A7D6E8;
      --modal-header-bg: #16202D;
      --modal-header-texto: #A7D6E8;
      --input-bg: #FFFFFF;
      --input-texto: #0D111B;
      --input-borde: #ced4da;
      --offcanvas-header-bg: #16202D;
      --offcanvas-header-texto: #A7D6E8;
      --offcanvas-body-bg: #F2F2F2;
    }

    .modo-oscuro {
      --fondo-principal: #0D111B;
      --fondo-card: #16202D;
      --texto-principal: #F2F2F2;
      --texto-secundario: #A7D6E8;
      --texto-muted: #8A8D91;
      --acento: #A7D6E8;
      --acento-claro: #39546A;
      --acento-hover: #8ec8db;
      --navbar-bg: #0D111B;
      --navbar-texto: #A7D6E8;
      --footer-bg: #0D111B;
      --footer-texto: #A7D6E8;
      --borde-suave: rgba(255, 255, 255, 0.08);
      --sombra-card: 0 3px 12px rgba(0, 0, 0, 0.3);
      --sombra-card-hover: 0 14px 28px rgba(0, 0, 0, 0.5);
      --badge-bg: #39546A;
      --badge-texto: #F2F2F2;
      --btn-flotante-bg: #A7D6E8;
      --btn-flotante-texto: #0D111B;
      --btn-flotante-hover: #8ec8db;
      --precio-pill-bg: #A7D6E8;
      --precio-pill-texto: #0D111B;
      --toast-bg: #16202D;
      --toast-texto: #F2F2F2;
      --toast-border: #A7D6E8;
      --skeleton-base: #1a2332;
      --skeleton-shine: #243044;
      --categoria-inactiva: #8A8D91;
      --categoria-hover: #A7D6E8;
      --categoria-activa: #A7D6E8;
      --modal-header-bg: #0D111B;
      --modal-header-texto: #A7D6E8;
      --input-bg: #16202D;
      --input-texto: #F2F2F2;
      --input-borde: #39546A;
      --offcanvas-header-bg: #0D111B;
      --offcanvas-header-texto: #A7D6E8;
      --offcanvas-body-bg: #0D111B;
    }

    * {
      -webkit-tap-highlight-color: transparent;
    }

    body {
      background-color: var(--fondo-principal);
      font-family: 'Inter', sans-serif;
      color: var(--texto-principal);
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    h1,
    h2,
    h3,
    .navbar-brand,
    .btn-categoria,
    .card-title,
    .fw-bold.fs-5,
    .offcanvas-title,
    .precio-pill {
      font-family: 'Poppins', sans-serif;
    }

    ::selection {
      background: var(--acento-claro);
      color: #0D111B;
    }

    a:focus-visible,
    button:focus-visible,
    input:focus-visible,
    select:focus-visible,
    textarea:focus-visible {
      outline: 3px solid var(--acento);
      outline-offset: 2px;
    }

    .navbar,
    .footer {
      background-color: var(--navbar-bg);
      transition: background-color 0.3s ease;
    }
    .navbar-brand,
    .nav-link,
    .btn-carrito {
      color: var(--navbar-texto) !important;
    }
    .navbar-brand {
      font-weight: 800;
      font-size: 1.35rem;
      letter-spacing: 0.2px;
    }

    .btn-guajiro {
      background: var(--acento);
      color: #F2F2F2;
      border: none;
      font-weight: 700;
      font-family: 'Poppins', sans-serif;
      border-radius: 2rem;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.2);
      transition: transform 0.15s ease, box-shadow 0.15s ease, background-color 0.2s ease;
    }
    .btn-guajiro:hover {
      background: var(--acento-hover);
      transform: translateY(-2px);
      box-shadow: 0 10px 22px rgba(0, 0, 0, 0.28);
      color: #fff;
    }
    .btn-guajiro:active {
      transform: translateY(0);
    }
    .btn-guajiro:disabled {
      opacity: 0.45;
      box-shadow: none;
      transform: none;
    }
    .modo-oscuro .btn-guajiro {
      background: var(--acento);
      color: #0D111B;
    }
    .modo-oscuro .btn-guajiro:hover {
      background: var(--acento-hover);
      color: #0D111B;
    }

    .card-producto {
      position: relative;
      border: none;
      border-radius: 1.1rem;
      box-shadow: var(--sombra-card);
      background: var(--fondo-card);
      transition: transform 0.2s ease, box-shadow 0.2s ease, background-color 0.3s ease;
    }
    .card-producto:hover {
      transform: translateY(-4px);
      box-shadow: var(--sombra-card-hover);
    }

    .producto-media {
      position: relative;
    }
    .producto-media img,
    .producto-media .producto-sin-foto {
      border-radius: 1.1rem 1.1rem 0 0;
    }
    .card-producto .card-img-top {
      aspect-ratio: 4/3;
      object-fit: cover;
      display: block;
    }
    .producto-sin-foto {
      width: 100%;
      aspect-ratio: 4/3;
      background-color: var(--navbar-bg);
      color: var(--acento-claro);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.6rem;
    }

    .card-body {
      padding-top: 1.7rem;
      border-radius: 0 0 1.1rem 1.1rem;
      color: var(--texto-principal);
    }
    .card-text {
      color: var(--texto-muted);
    }

    .btn-add-flotante {
      position: absolute;
      right: 14px;
      bottom: -18px;
      z-index: 3;
      width: 44px;
      height: 44px;
      border-radius: 50%;
      background: var(--btn-flotante-bg);
      color: var(--btn-flotante-texto);
      border: 3px solid var(--fondo-card);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.25rem;
      box-shadow: 0 6px 14px rgba(0, 0, 0, 0.3);
      transition: transform 0.15s ease, background-color 0.2s ease;
    }
    .btn-add-flotante:hover {
      transform: scale(1.12) rotate(8deg);
      background: var(--btn-flotante-hover);
    }
    .btn-add-flotante:active {
      transform: scale(0.92);
    }
    .btn-add-flotante:disabled {
      background: #8A8D91;
      box-shadow: none;
      color: #F2F2F2;
    }

    .precio-pill {
      background: var(--precio-pill-bg);
      color: var(--precio-pill-texto);
      font-weight: 800;
      border-radius: 2rem;
      padding: 0.3rem 0.85rem;
      font-size: 1.05rem;
      display: inline-block;
    }

    .badge-oferta {
      position: absolute;
      top: 10px;
      left: 10px;
      z-index: 2;
      background: var(--badge-bg);
      color: var(--badge-texto);
      font-weight: 700;
      border-radius: 2rem;
      padding: 0.3rem 0.7rem;
      font-size: 0.78rem;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    }

    .offcanvas-header,
    .offcanvas-footer {
      background-color: var(--offcanvas-header-bg);
      color: var(--offcanvas-header-texto);
      transition: background-color 0.3s ease;
    }
    .offcanvas-body {
      background-color: var(--offcanvas-body-bg);
      color: var(--texto-principal);
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .toast {
      background-color: var(--toast-bg);
      color: var(--toast-texto);
      border: 1px solid var(--toast-border);
      border-radius: 0.8rem;
    }
    .toast i.bi-check-circle-fill {
      color: var(--acento-claro);
    }
    .toast .btn-close {
      filter: invert(1);
    }
    .toast .toast-body {
      font-size: 0.9rem;
      line-height: 1.3;
    }
    .modo-oscuro .toast .btn-close {
      filter: invert(0);
    }

    .error-message {
      display: none;
    }

    .categorias-nav {
      background-color: var(--navbar-bg);
      padding: 0.6rem 0;
      position: sticky;
      top: 56px;
      z-index: 1020;
      overflow-x: auto;
      white-space: nowrap;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      -ms-overflow-style: none;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.25);
      transition: background-color 0.3s ease;
    }
    .categorias-nav::-webkit-scrollbar {
      display: none;
    }
    .categorias-nav .container {
      display: flex;
      flex-wrap: nowrap;
      padding: 0 1rem;
      gap: 0.4rem;
    }
    .btn-categoria {
      display: inline-block;
      background: transparent;
      color: var(--categoria-inactiva);
      border: none;
      border-bottom: 3px solid transparent;
      border-radius: 0;
      padding: 0.5rem 0.9rem;
      margin: 0;
      font-weight: 600;
      font-size: 0.88rem;
      transition: all 0.2s ease;
      white-space: nowrap;
      text-decoration: none;
    }
    .btn-categoria:hover {
      color: var(--categoria-hover);
      border-bottom-color: rgba(167, 214, 232, 0.4);
    }
    .btn-categoria.active {
      background: transparent;
      color: var(--categoria-activa);
      border-bottom-color: var(--categoria-activa);
      font-weight: 700;
      box-shadow: none;
    }

    .icono-titulo {
      background: var(--acento);
      color: #F2F2F2;
      width: 38px;
      height: 38px;
      border-radius: 50%;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      font-size: 1.1rem;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.25);
    }
    .modo-oscuro .icono-titulo {
      background: var(--acento-claro);
      color: #0D111B;
    }

    .carrito-item {
      border: none !important;
      border-bottom: 1px solid var(--borde-suave) !important;
      padding: 0.9rem 0.25rem;
      background: transparent;
      color: var(--texto-principal);
    }
    .stepper {
      display: flex;
      align-items: center;
      gap: 0.6rem;
    }
    .stepper button {
      width: 28px;
      height: 28px;
      border-radius: 50%;
      border: 1px solid var(--borde-suave);
      background: var(--fondo-card);
      color: var(--texto-principal);
      display: flex;
      align-items: center;
      justify-content: center;
      line-height: 1;
      transition: background-color 0.15s ease, color 0.15s ease, border-color 0.15s ease;
    }
    .stepper button:hover {
      background: var(--acento);
      color: #F2F2F2;
      border-color: transparent;
    }
    .modo-oscuro .stepper button:hover {
      color: #0D111B;
    }

    #cart-count {
      background-color: var(--acento-claro) !important;
      color: #0D111B !important;
    }
    #cart-count.bump {
      animation: bump 0.3s ease;
    }
    @keyframes bump {
      0% {
        transform: scale(1);
      }
      40% {
        transform: scale(1.4);
      }
      100% {
        transform: scale(1);
      }
    }

    .carrito-vacio {
      text-align: center;
      padding: 2.5rem 1rem;
      color: var(--texto-muted);
    }
    .carrito-vacio i {
      font-size: 2.4rem;
      color: var(--acento);
    }

    #total-pedido {
      color: var(--acento);
      font-size: 1.15rem;
    }

    .desglose {
      font-size: 0.9rem;
      color: var(--texto-muted);
    }
    .desglose span {
      color: var(--texto-principal);
      font-weight: 500;
    }

    .skeleton {
      background: linear-gradient(90deg, var(--skeleton-base) 25%, var(--skeleton-shine) 50%, var(--skeleton-base) 75%);
      background-size: 200% 100%;
      animation: skeleton-pulse 1.4s ease-in-out infinite;
      border-radius: 0.6rem;
    }
    @keyframes skeleton-pulse {
      0% {
        background-position: 200% 0;
      }
      100% {
        background-position: -200% 0;
      }
    }
    .skeleton-card {
      height: 270px;
      border-radius: 1rem;
    }
    .skeleton-chip {
      height: 32px;
      width: 84px;
      border-radius: 2rem;
      display: inline-block;
      margin-right: 0.4rem;
      flex: none;
    }

    /* Toggle modo oscuro */
    .modo-toggle {
      background: transparent;
      border: 1px solid var(--acento-claro);
      color: var(--acento-claro);
      border-radius: 2rem;
      padding: 0.35rem 0.8rem;
      font-size: 0.85rem;
      cursor: pointer;
      transition: all 0.2s ease;
      display: flex;
      align-items: center;
      gap: 0.3rem;
    }
    .modo-toggle:hover {
      background: var(--acento-claro);
      color: #0D111B;
    }

    .form-select,
    .form-control {
      background-color: var(--input-bg);
      color: var(--input-texto);
      border-color: var(--input-borde);
      transition: background-color 0.3s ease, color 0.3s ease;
    }
    .form-select:focus,
    .form-control:focus {
      background-color: var(--input-bg);
      color: var(--input-texto);
      border-color: var(--acento);
      box-shadow: 0 0 0 0.2rem rgba(57, 84, 106, 0.25);
    }

    .modal-content {
      background-color: var(--fondo-card);
      color: var(--texto-principal);
    }
    .modal-header {
      background-color: var(--modal-header-bg);
      color: var(--modal-header-texto);
      border-bottom: 1px solid var(--borde-suave);
    }
    .modal-footer {
      border-top: 1px solid var(--borde-suave);
    }

    .list-group-item {
      background-color: transparent;
    }

    @media (max-width: 280px) {
      .toast .toast-body {
        font-size: 0.8rem;
        padding: 0.5rem;
      }
      .toast .btn-close {
        font-size: 0.7rem;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        transition-duration: 0.01ms !important;
      }
    }

    .toast-container {
      display: flex;
      justify-content: center;
      left: 0;
      right: 0;
      pointer-events: none;
    }
    .toast {
      max-width: 90vw;
      margin: 0 auto;
      pointer-events: auto;
    }

    /* Clase para texto muted */
    .text-muted {
      color: var(--texto-muted) !important;
    }
    .modo-oscuro .btn-close-white {
      filter: invert(1);
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-dark sticky-top">
    <div class="container">
      <a class="navbar-brand fw-bold d-flex align-items-center" href="#">
        <img src="guajiro-icon.png" alt="GUAJIRO" style="height: 30px; width: 30px; object-fit: contain; margin-right: 0.5rem;">
        GUAJIRO
      </a>
      <div class="d-flex align-items-center gap-2">
        <button class="modo-toggle" id="modo-toggle" title="Cambiar modo claro/oscuro">
          <i class="bi bi-moon-fill" id="modo-icono"></i>
          <span id="modo-texto">Oscuro</span>
        </button>
        <button class="btn btn-outline-light position-relative" type="button" data-bs-toggle="offcanvas" data-bs-target="#carritoOffcanvas" style="border-color: var(--acento-claro); color: var(--acento-claro);">
          <i class="bi bi-cart-fill"></i>
          <span id="cart-count" class="position-absolute top-0 start-100 translate-middle badge rounded-pill">0</span>
        </button>
      </div>
    </div>
  </nav>

  <div class="categorias-nav" id="categorias-nav">
    <div class="container" id="categorias-container">
      <span class="skeleton skeleton-chip"></span>
      <span class="skeleton skeleton-chip"></span>
      <span class="skeleton skeleton-chip"></span>
    </div>
  </div>

  <div id="horario-aviso" class="alert text-center mb-0 d-none" role="alert" style="background-color: var(--acento-claro); color: #0D111B; border: none; border-radius: 0;">
    <i class="bi bi-clock"></i> Fuera de horario. <span id="horario-texto"></span>
  </div>

  <div aria-live="polite" aria-atomic="true" class="toast-container position-fixed bottom-0 start-0 end-0 p-3" style="z-index: 11">
    <div id="toastPedido" class="toast align-items-center" role="alert" data-bs-delay="5000">
      <div class="d-flex">
        <div class="toast-body">
          <i class="bi bi-check-circle-fill"></i> ¡Pedido enviado!<br><small style="color: var(--acento-claro);">Te contactaremos pronto.</small>
        </div>
        <button type="button" class="btn-close me-2 m-auto" data-bs-dismiss="toast"></button>
      </div>
    </div>
  </div>

  <main class="container my-4">
    <h2 class="mb-3 d-flex align-items-center gap-2">
      <span class="icono-titulo"><i class="bi bi-snow2"></i></span>
      Nuestros Congelados
    </h2>
    <div id="productos-container" class="row row-cols-1 row-cols-md-3 g-4">
      <div class="col"><div class="skeleton skeleton-card"></div></div>
      <div class="col"><div class="skeleton skeleton-card"></div></div>
      <div class="col"><div class="skeleton skeleton-card"></div></div>
    </div>
  </main>

  <footer class="footer text-center py-3 mt-5">
    <div class="container">
      <div class="d-flex flex-wrap justify-content-center align-items-center gap-3">
        <small style="color: var(--footer-texto);">© 2025 GUAJIRO</small>
        <a href="tel:+5356553393" class="text-decoration-none me-2" style="color: var(--footer-texto);" title="Llamar">
          <i class="bi bi-telephone-fill"></i> 5 6553393
        </a>
        <a href="https://wa.me/5356553393" target="_blank" class="text-decoration-none me-3" style="color: var(--footer-texto);" title="WhatsApp">
          <i class="bi bi-whatsapp"></i>
        </a>
        <a href="tel:+5353866494" class="text-decoration-none me-2" style="color: var(--footer-texto);" title="Llamar">
          <i class="bi bi-telephone-fill"></i> 5 3866494
        </a>
        <a href="https://wa.me/5353866494" target="_blank" class="text-decoration-none" style="color: var(--footer-texto);" title="WhatsApp">
          <i class="bi bi-whatsapp"></i>
        </a>
      </div>
    </div>
  </footer>

  <div class="offcanvas offcanvas-end" tabindex="-1" id="carritoOffcanvas">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title"><i class="bi bi-cart2"></i> Tu pedido</h5>
      <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body">
      <div id="carrito-lista" class="list-group mb-3"></div>
      <div class="mb-3">
        <label class="form-label fw-bold">Método de pago</label>
        <select id="metodo-pago" class="form-select" onchange="actualizarCarrito()">
          <option value="efectivo">Efectivo</option>
          <option value="transferencia">Transferencia</option>
        </select>
        <small id="recargo-info" class="d-none" style="color: var(--texto-muted);"></small>
      </div>
      <div class="mb-3">
        <label class="form-label fw-bold">Reparto</label>
        <select id="reparto-select" class="form-select" onchange="actualizarCarrito()">
          <option value="">Selecciona tu reparto...</option>
        </select>
      </div>
      <div class="desglose mb-1">
        Subtotal: <span id="subtotal-carrito">0.00</span> CUP
      </div>
      <div id="recargo-desglose" class="desglose mb-1 d-none">
        Recargo transf.: <span id="recargo-aplicado">0.00</span> CUP
      </div>
      <div id="envio-desglose" class="desglose mb-1 d-none">
        Envío: <span id="envio-aplicado">0.00</span> CUP
      </div>
      <div class="d-flex justify-content-between fw-bold border-top pt-2" style="border-color: var(--borde-suave) !important;">
        <span>Total:</span>
        <span id="total-pedido">0.00 CUP</span>
      </div>
      <button id="btn-checkout" class="btn btn-guajiro w-100 mt-3" disabled>
        <i class="bi bi-check-circle"></i> Realizar pedido
      </button>
    </div>
  </div>

  <div class="modal fade" id="checkoutModal" tabindex="-1">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title"><i class="bi bi-pencil-square"></i> Datos de entrega</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <div id="checkout-error" class="alert alert-danger py-2 error-message" role="alert">
            <i class="bi bi-exclamation-triangle"></i> <span id="error-text"></span>
          </div>
          <form id="checkout-form">
            <div class="mb-3"><label class="form-label">Nombre</label><input type="text" class="form-control" id="nombre" required></div>
            <div class="mb-3"><label class="form-label">Teléfono</label><input type="tel" class="form-control" id="telefono" required></div>
            <div class="mb-3"><label class="form-label">Dirección completa</label><textarea class="form-control" id="direccion" rows="2" required></textarea></div>
            <div class="mb-3"><label class="form-label">Punto de referencia <small class="text-muted">(opcional)</small></label><textarea class="form-control" id="referencia" rows="1" placeholder="Ej: detrás de la gasolinera..."></textarea></div>
          </form>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          <button type="button" class="btn btn-guajiro" id="confirmar-pedido"><i class="bi bi-check-lg"></i> Confirmar pedido</button>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    (function() { var noop = function() {}; console.log = noop; console.warn = noop; console.error = noop; console.info = noop; })();

    const SUPABASE_URL = 'https://xjjbrnjgpncxwqishseo.supabase.co';
    const SUPABASE_ANON_KEY = 'sb_publishable_e68llCL_CYuf2M9TyVcdWA_HdMDYpVF';
    const supabaseClient = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
    const NTFY_TOPIC = 'comecome_8f3js9f83jf93jf';

    let todasCategorias = [];
    let todosProductos = [];
    let hayCombosActivos = false;
    let categoriaActiva = 'todas';
    let carrito = [];
    let checkoutModalInstance = null;
    let horarioAbierto = true;
    let recargoTransferencia = 0;

    // Modo oscuro
    const modoToggle = document.getElementById('modo-toggle');
    const modoIcono = document.getElementById('modo-icono');
    const modoTexto = document.getElementById('modo-texto');

    function aplicarModo(oscuro) {
      if (oscuro) {
        document.body.classList.add('modo-oscuro');
        modoIcono.className = 'bi bi-sun-fill';
        modoTexto.textContent = 'Claro';
      } else {
        document.body.classList.remove('modo-oscuro');
        modoIcono.className = 'bi bi-moon-fill';
        modoTexto.textContent = 'Oscuro';
      }
    }

    const modoGuardado = localStorage.getItem('guajiro-modo-oscuro');
    if (modoGuardado === 'true') {
      aplicarModo(true);
    } else {
      aplicarModo(false);
    }

    modoToggle.addEventListener('click', () => {
      const esOscuro = document.body.classList.contains('modo-oscuro');
      const nuevoModo = !esOscuro;
      aplicarModo(nuevoModo);
      localStorage.setItem('guajiro-modo-oscuro', nuevoModo);
    });

    document.addEventListener('DOMContentLoaded', async () => {
      const modalEl = document.getElementById('checkoutModal');
      if (modalEl) checkoutModalInstance = new bootstrap.Modal(modalEl);
      await cargarConfiguracion();
      await cargarCategorias();
      await cargarProductos();
      await verificarCombosActivos();
      await cargarRepartosEnvio();
      renderCategorias();
      await verificarHorario();
      renderProductos();
      document.getElementById('metodo-pago').addEventListener('change', () => {
        actualizarInfoRecargo();
        actualizarCarrito();
      });
      setInterval(async () => {
        await verificarHorario();
        if (categoriaActiva === 'combos') {
          cargarCombosPublicos();
        } else {
          renderProductos();
        }
      }, 60000);
    });

    async function cargarConfiguracion() {
      const { data, error } = await supabaseClient.from('configuracion').select('recargo_transferencia').single();
      if (data) {
        recargoTransferencia = parseFloat(data.recargo_transferencia) || 0;
        actualizarInfoRecargo();
      }
    }

    function actualizarInfoRecargo() {
      const metodo = document.getElementById('metodo-pago').value;
      const info = document.getElementById('recargo-info');
      if (metodo === 'transferencia' && recargoTransferencia > 0) {
        info.textContent = `Se aplica un recargo del ${recargoTransferencia}% sobre el subtotal.`;
        info.classList.remove('d-none');
      } else {
        info.classList.add('d-none');
      }
    }

    async function cargarCategorias() {
      const { data } = await supabaseClient.from('categorias').select('*').order('nombre');
      if (data) todasCategorias = data;
    }

    async function cargarProductos() {
      const { data } = await supabaseClient.from('productos').select('*, categorias(nombre)').eq('activo', true).order('nombre');
      if (data) todosProductos = data;
    }

    async function verificarCombosActivos() {
      const { data } = await supabaseClient.from('combos').select('id').eq('activo', true).limit(1);
      hayCombosActivos = data && data.length > 0;
    }

    async function cargarRepartosEnvio() {
      const select = document.getElementById('reparto-select');
      const { data, error } = await supabaseClient.from('repartos').select('*').eq('activo', true).order('nombre');
      select.innerHTML = '<option value="">Selecciona tu reparto...</option>';
      if (error) {
        select.innerHTML = '<option value="">Error al cargar repartos</option>';
        return;
      }
      if (!data || data.length === 0) {
        select.innerHTML = '<option value="">No hay repartos disponibles</option>';
        return;
      }
      data.forEach(r => {
        const opt = document.createElement('option');
        opt.value = r.id;
        opt.dataset.precio = r.precio;
        opt.textContent = `${r.nombre} (${r.distancia}) +${parseFloat(r.precio).toFixed(2)} CUP`;
        select.appendChild(opt);
      });
    }

    function renderCategorias() {
      const container = document.getElementById('categorias-container');
      let html = `<button class="btn-categoria active" onclick="filtrarPorCategoria('todas', this)">Todas</button>`;
      if (hayCombosActivos) {
        html += `<button class="btn-categoria" onclick="filtrarPorCategoria('combos', this)">Combos</button>`;
      }
      const categoriasConProductos = todasCategorias.filter(cat =>
        todosProductos.some(prod => prod.categoria_id === cat.id)
      );
      categoriasConProductos.forEach(cat => {
        html += `<button class="btn-categoria" onclick="filtrarPorCategoria('${cat.id}', this)">${cat.nombre}</button>`;
      });
      container.innerHTML = html;
    }

    function filtrarPorCategoria(catId, el) {
      categoriaActiva = catId;
      document.querySelectorAll('.btn-categoria').forEach(btn => btn.classList.remove('active'));
      el.classList.add('active');
      if (catId === 'combos') {
        cargarCombosPublicos();
      } else {
        renderProductos();
      }
    }

    function renderProductos() {
      const container = document.getElementById('productos-container');
      let productosFiltrados = todosProductos;
      if (categoriaActiva !== 'todas') {
        productosFiltrados = todosProductos.filter(p => p.categoria_id === categoriaActiva);
      }
      if (productosFiltrados.length === 0) {
        container.innerHTML = '<p class="text-muted">No hay productos en esta categoría.</p>';
        return;
      }
      container.innerHTML = productosFiltrados.map(p => `
        <div class="col">
          <div class="card card-producto h-100">
            <div class="producto-media">
              ${p.foto_url
                ? `<img src="${p.foto_url}" class="card-img-top" alt="${p.nombre}">`
                : `<div class="producto-sin-foto"><i class="bi bi-snow2"></i></div>`
              }
              <button class="btn-add-flotante" onclick="agregarAlCarrito('${p.id}')" ${horarioAbierto ? '' : 'disabled'} aria-label="Añadir ${p.nombre} al carrito">
                <i class="bi bi-plus-lg"></i>
              </button>
            </div>
            <div class="card-body">
              <h5 class="card-title">${p.nombre}</h5>
              <p class="card-text small">${p.descripcion || ''}</p>
              <span class="precio-pill">${parseFloat(p.precio).toFixed(2)} CUP</span>
            </div>
          </div>
        </div>
      `).join('');
    }

    async function cargarCombosPublicos() {
      const container = document.getElementById('productos-container');
      container.innerHTML = '<div class="col"><div class="skeleton skeleton-card"></div></div><div class="col"><div class="skeleton skeleton-card"></div></div><div class="col"><div class="skeleton skeleton-card"></div></div>';
      const { data: combos, error } = await supabaseClient.from('combos').select('*').eq('activo', true).order('nombre');
      if (error || !combos || combos.length === 0) {
        container.innerHTML = '<p class="text-muted">No hay combos disponibles.</p>';
        return;
      }
      let html = '';
      for (const combo of combos) {
        const { data: items } = await supabaseClient.from('combo_items').select('*, productos(nombre, precio)').eq('combo_id', combo.id);
        const totalOriginal = (items || []).reduce((s, i) => s + (parseFloat(i.productos.precio) * i.cantidad), 0);
        let precioFinal = totalOriginal;
        if (combo.tipo_descuento === 'porcentaje') {
          precioFinal = totalOriginal * (1 - combo.valor_descuento / 100);
        } else if (combo.tipo_descuento === 'fijo') {
          precioFinal = parseFloat(combo.valor_descuento);
        }
        const listaProductos = (items || []).map(i => `${i.cantidad}x ${i.productos.nombre}`).join(', ');
        html += `
          <div class="col">
            <div class="card card-producto h-100">
              <div class="producto-media">
                <div class="producto-sin-foto"><i class="bi bi-star-fill"></i></div>
                <span class="badge-oferta">${combo.tipo_descuento === 'porcentaje' ? '-' + combo.valor_descuento + '%' : 'Oferta'}</span>
                <button class="btn-add-flotante" onclick="agregarComboAlCarrito('${combo.id}')" ${horarioAbierto ? '' : 'disabled'} aria-label="Añadir combo ${combo.nombre} al carrito">
                  <i class="bi bi-plus-lg"></i>
                </button>
              </div>
              <div class="card-body">
                <h5 class="card-title">${combo.nombre}</h5>
                <p class="card-text small">${listaProductos}</p>
                <div class="d-flex align-items-center gap-2">
                  <s class="text-muted small">${totalOriginal.toFixed(2)} CUP</s>
                  <span class="precio-pill">${precioFinal.toFixed(2)} CUP</span>
                </div>
              </div>
            </div>
          </div>`;
      }
      container.innerHTML = html;
    }

    async function verificarHorario() {
      const ahora = new Date();
      const diaSemana = ahora.getDay();
      const horaActual = ahora.getHours() + ahora.getMinutes() / 60;
      const { data } = await supabaseClient.from('horarios').select('abierto, hora_apertura, hora_cierre').eq('dia_semana', diaSemana).single();
      let textoHorario = 'Cerrado hoy.';
      if (data) {
        if (data.abierto) {
          horarioAbierto = true;
          textoHorario = '';
        } else if (data.hora_apertura && data.hora_cierre && data.hora_apertura !== data.hora_cierre) {
          const [hA, mA] = data.hora_apertura.split(':').map(Number);
          const [hC, mC] = data.hora_cierre.split(':').map(Number);
          const apertura = hA + mA / 60;
          const cierre = hC + mC / 60;
          horarioAbierto = horaActual >= apertura && horaActual < cierre;
          if (!horarioAbierto) {
            textoHorario = `Los pedidos en línea están disponibles de ${data.hora_apertura.slice(0,5)} a ${data.hora_cierre.slice(0,5)}.`;
          }
        } else {
          horarioAbierto = false;
          textoHorario = 'Cerrado hoy.';
        }
      } else {
        horarioAbierto = false;
        textoHorario = 'Cerrado hoy.';
      }
      document.getElementById('horario-aviso').classList.toggle('d-none', horarioAbierto);
      document.getElementById('horario-texto').textContent = textoHorario;
    }

    function agregarAlCarrito(idProducto) {
      const producto = todosProductos.find(p => p.id === idProducto);
      if (!producto) return;
      const grupo = carrito.find(item => item.id === idProducto && !item.esCombo);
      if (grupo) {
        grupo.cantidad++;
      } else {
        carrito.push({
          id: producto.id,
          nombre: producto.nombre,
          precio: parseFloat(producto.precio),
          foto: producto.foto_url,
          permiteExtras: producto.permite_extras,
          cantidad: 1,
          extras: '',
          esCombo: false
        });
      }
      actualizarCarrito();
    }

    async function agregarComboAlCarrito(comboId) {
      const { data: combo } = await supabaseClient.from('combos').select('*').eq('id', comboId).single();
      if (!combo) return;
      const { data: items } = await supabaseClient.from('combo_items').select('*, productos(nombre, precio)').eq('combo_id', comboId);
      const totalOriginal = (items || []).reduce((s, i) => s + (parseFloat(i.productos.precio) * i.cantidad), 0);
      let precioFinal = totalOriginal;
      if (combo.tipo_descuento === 'porcentaje') {
        precioFinal = totalOriginal * (1 - combo.valor_descuento / 100);
      } else if (combo.tipo_descuento === 'fijo') {
        precioFinal = parseFloat(combo.valor_descuento);
      }
      const comboItem = { id: combo.id, nombre: combo.nombre, precio: precioFinal, permiteExtras: false, cantidad: 1, extras: '', esCombo: true };
      const grupo = carrito.find(item => item.id === combo.id && item.esCombo);
      if (grupo) {
        grupo.cantidad++;
      } else {
        carrito.push(comboItem);
      }
      actualizarCarrito();
    }

    function cambiarCantidad(index, delta) {
      if (index < 0 || index >= carrito.length) return;
      carrito[index].cantidad += delta;
      if (carrito[index].cantidad <= 0) carrito.splice(index, 1);
      actualizarCarrito();
    }

    function actualizarCarrito() {
      const lista = document.getElementById('carrito-lista');
      const totalSpan = document.getElementById('total-pedido');
      const countBadge = document.getElementById('cart-count');
      const btnCheckout = document.getElementById('btn-checkout');
      lista.innerHTML = carrito.map((item, index) => `
        <div class="list-group-item carrito-item d-flex justify-content-between align-items-start">
          <div class="w-100">
            <div class="d-flex justify-content-between align-items-start">
              <strong>${item.nombre} ${item.esCombo ? '<small style="color: var(--acento-claro);">(Combo)</small>' : ''}</strong>
              <span class="fw-bold">${(item.precio * item.cantidad).toFixed(2)} CUP</span>
            </div>
            <div class="d-flex justify-content-between align-items-center mt-2">
              <div class="stepper">
                <button type="button" onclick="cambiarCantidad(${index}, -1)" aria-label="Quitar una unidad"><i class="bi bi-dash"></i></button>
                <span>${item.cantidad}</span>
                <button type="button" onclick="cambiarCantidad(${index}, 1)" aria-label="Añadir una unidad"><i class="bi bi-plus"></i></button>
              </div>
              <button class="btn btn-sm text-danger" onclick="eliminarDelCarrito(${index})" title="Eliminar"><i class="bi bi-trash"></i></button>
            </div>
            ${!item.esCombo && item.permiteExtras ? `<input type="text" class="form-control form-control-sm mt-2" placeholder="Instrucciones especiales" value="${item.extras}" oninput="actualizarExtras(${index}, this.value)">` : ''}
          </div>
        </div>
      `).join('') || `<div class="carrito-vacio"><i class="bi bi-basket"></i><p class="mt-2 mb-0 fw-semibold">Tu carrito está vacío</p><small>Añade algo rico de la carta 🧊</small></div>`;
      const subtotal = carrito.reduce((sum, item) => sum + (item.precio * item.cantidad), 0);
      document.getElementById('subtotal-carrito').textContent = subtotal.toFixed(2);
      const metodoPago = document.getElementById('metodo-pago').value;
      let recargo = 0;
      if (metodoPago === 'transferencia' && recargoTransferencia > 0) {
        recargo = subtotal * (recargoTransferencia / 100);
        document.getElementById('recargo-aplicado').textContent = recargo.toFixed(2);
        document.getElementById('recargo-desglose').classList.remove('d-none');
      } else {
        document.getElementById('recargo-desglose').classList.add('d-none');
      }
      const repartoSelect = document.getElementById('reparto-select');
      let envio = 0;
      if (repartoSelect && repartoSelect.selectedOptions[0]?.dataset.precio) {
        envio = parseFloat(repartoSelect.selectedOptions[0].dataset.precio);
        document.getElementById('envio-aplicado').textContent = envio.toFixed(2);
        document.getElementById('envio-desglose').classList.remove('d-none');
      } else {
        document.getElementById('envio-desglose').classList.add('d-none');
      }
      const total = subtotal + recargo + envio;
      totalSpan.textContent = total.toFixed(2) + ' CUP';
      const nuevoConteo = carrito.reduce((sum, item) => sum + item.cantidad, 0);
      if (countBadge.textContent !== String(nuevoConteo)) {
        countBadge.classList.remove('bump');
        void countBadge.offsetWidth;
        countBadge.classList.add('bump');
      }
      countBadge.textContent = nuevoConteo;
      btnCheckout.disabled = carrito.length === 0 || !repartoSelect?.value;
    }

    function actualizarExtras(index, valor) {
      if (index >= 0 && index < carrito.length) carrito[index].extras = valor;
    }

    function eliminarDelCarrito(index) {
      carrito.splice(index, 1);
      actualizarCarrito();
    }

    document.getElementById('btn-checkout').addEventListener('click', () => {
      if (carrito.length === 0) return;
      checkoutModalInstance.show();
    });

    document.getElementById('confirmar-pedido').addEventListener('click', async () => {
      const nombre = document.getElementById('nombre').value.trim();
      const telefono = document.getElementById('telefono').value.trim();
      const direccion = document.getElementById('direccion').value.trim();
      const referencia = document.getElementById('referencia').value.trim();
      const errorDiv = document.getElementById('checkout-error');
      const errorText = document.getElementById('error-text');
      if (!nombre || !telefono || !direccion) {
        errorText.textContent = 'Completa los campos obligatorios.';
        errorDiv.style.display = 'block';
        return;
      }
      errorDiv.style.display = 'none';
      const metodoPago = document.getElementById('metodo-pago').value;
      const repartoSelect = document.getElementById('reparto-select');
      const opcion = repartoSelect.options[repartoSelect.selectedIndex];
      const zonaTexto = opcion && opcion.value ? opcion.textContent : 'Domicilio';
      const totalPedido = parseFloat(document.getElementById('total-pedido').textContent);
      const { error: errorPedido } = await supabaseClient.from('pedidos').insert([{
        nombre, telefono, direccion, referencia: referencia || null, metodo_pago: metodoPago,
        zona: zonaTexto, total: totalPedido,
        items: carrito.map(item => ({ nombre: item.nombre, precio: item.precio, cantidad: item.cantidad, extras: item.extras || null, esCombo: item.esCombo || false })),
        estado: 'pendiente'
      }]);
      if (errorPedido) {
        errorText.textContent = 'Error al enviar el pedido.';
        errorDiv.style.display = 'block';
        return;
      }
      const productosTexto = carrito.map(item => {
        const extra = item.extras ? ` (${item.extras})` : '';
        return `${item.cantidad}x ${item.nombre}${extra} — ${(item.precio * item.cantidad).toFixed(2)} CUP`;
      }).join('\n');
      const mensajeNtfy = `🧊 NUEVO PEDIDO — GUAJIRO\n👤 ${nombre}\n📞 ${telefono}\n📍 ${direccion}${referencia ? '\n📌 Ref: ' + referencia : ''}\n🛵 Entrega: ${zonaTexto}\n💳 Pago: ${metodoPago}\n────────────────\n${productosTexto}\n────────────────\n💰 TOTAL: ${totalPedido.toFixed(2)} CUP`;
      fetch(`https://ntfy.sh/${NTFY_TOPIC}`, { method: 'POST', body: mensajeNtfy }).catch(() => {});
      checkoutModalInstance.hide();
      const offcanvas = bootstrap.Offcanvas.getInstance(document.getElementById('carritoOffcanvas'));
      if (offcanvas) offcanvas.hide();
      document.getElementById('confirmar-pedido').blur();
      setTimeout(() => {
        const toast = new bootstrap.Toast(document.getElementById('toastPedido'));
        toast.show();
      }, 300);
      carrito = [];
      actualizarCarrito();
    });
  </script>
</body>
</html>